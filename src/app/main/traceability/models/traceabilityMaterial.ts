import { TraceabilityMaterialType } from './traceabilityMaterialType';

export class TraceabilityMaterial {
  id: number;
  color: string;
  shape: string;
  materialType: TraceabilityMaterialType;
}
